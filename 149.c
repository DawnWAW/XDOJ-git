#include<stdio.h>
#include<math.h>
int main(){
    // double n;
    // double sum=0;
    // scanf("%lf", &n);
    // for (int i = 1; i <= n; i++)
    // {
    //     sum += pow(-1.0, i + 1) * i / (2 * i - 1);
    // }
    // printf("%.3lf", sum);
   
    return 0;
}